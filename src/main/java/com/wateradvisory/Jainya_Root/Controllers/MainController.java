package com.wateradvisory.Jainya_Root.Controllers;

/**
 * Root controller for MainView.fxml. Each tab's content is its own
 * separate FXML + controller (DailyView, SeasonalView, CompareView),
 * included via fx:include, so this class has nothing to wire up itself.
 */
public class MainController {
}
